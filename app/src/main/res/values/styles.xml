<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
<!--        <item name="colorPrimary">@color/colorPrimary</item>-->
<!--        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
<!--        <item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="AppTheme.Camera"/>
    <style name="AppTheme.Camera.Title" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/clothesName</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="AppTheme.Camera.GuideTitle" parent="AppTheme.Camera.Title">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
